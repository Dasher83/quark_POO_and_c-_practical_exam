namespace WholesaleCloths.Shared
{
    internal class Enums
    {
        internal enum GarmentTypeEnum : byte
        {
            Shirt,
            Pants
        };

        internal enum GarmentQualityEnum : byte
        {
            Standard,
            Premium
        };

        internal enum SleeveTypeEnum : byte
        {
            Short,
            Long
        };

        internal enum NeckTypeEnum : byte
        {
            Standard,
            Mao
        };

        internal enum PantsTypeEnum : byte
        {
            Standard,
            SlimFit
        };
    }
}
